class Solution {
public:
    int largestAltitude(vector<int>& gain) {
        int mini=0;
        int sum=0;
        for(int i=0;i<gain.size();i++)
        {
            sum+=gain[i];
            mini=max(sum,mini);
        
        }
        return mini;
    }
};
